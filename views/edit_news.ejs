<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไขข่าว - สาขาวิศวกรรมคอมพิวเตอร์</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #f0f2f5;
        }
        .navbar {
            background-color: #3b5998 !important;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand, .nav-link {
            color: #ffffff !important;
        }
        .card {
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <%- include('nav', { user: user, currentPage: 'admin' }) %>

    <div class="container mt-5">
        <h1 class="mb-4">แก้ไขข่าว</h1>
        <div class="card">
            <div class="card-body">
                <form action="/admin/update-news/<%= news.id %>" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="title" class="form-label">หัวข้อข่าว</label>
                        <input type="text" class="form-control" id="title" name="title" value="<%= news.title %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="content" class="form-label">เนื้อหาข่าว</label>
                        <textarea class="form-control" id="content" name="content" rows="5" required><%= news.content %></textarea>
                    </div>
                    <div class="mb-3">
                        <label for="image" class="form-label">รูปภาพประกอบ</label>
                        <input type="file" class="form-control" id="image" name="image" accept="image/*">
                        <% if (news.image_url) { %>
                            <img src="<%= news.image_url %>" alt="Current image" class="mt-2" style="max-width: 200px;">
                        <% } %>
                    </div>
                    <div class="mb-3">
                        <label for="reference" class="form-label">ลิงค์อ้างอิง</label>
                        <input type="url" class="form-control" id="reference" name="reference" value="<%= news.reference %>">
                        <% if (news.reference) { %>
                            <a href="<%= news.reference %>" target="_blank" class="btn btn-link mt-2">ดูลิงค์อ้างอิง</a>
                        <% } %>
                    </div>
                    <button type="submit" class="btn btn-primary">บันทึกการแก้ไข</button>
                    <a href="/admin/news" class="btn btn-secondary">ยกเลิก</a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>