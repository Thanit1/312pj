<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ขอเข้าใช้ห้อง</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #f0f2f5;
        }
        .navbar {
            background-color: #3b5998 !important;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand, .nav-link {
            color: #ffffff !important;
        }
        .card {
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .btn-primary {
            background-color: #3b5998;
            border: none;
        }
        .btn-primary:hover {
            background-color: #2d4373;
        }
        .display-6 {
            font-size: 2.5rem;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <%- include('nav', { user: user, currentPage: 'index' }) %>
    <div class="container mt-5">
        <div class="card shadow-sm col-md-8 mx-auto">
            <div class="card-body p-4">
                <h2 class="mb-3 text-center text-primary fs-4">ยินดีต้อนรับ: <%= user.firstname %></h2>
                <h1 class="mb-3 text-center text-success fs-3">การขอเข้าใช้ห้อง 312</h1>
                <p class="mb-3 text-center text-muted fst-italic small">รหัสสามารถใช้ได้เพียงครั้งเดียว</p>
                <% if (typeof activeCode !== 'undefined' && activeCode) { %>
                    <div class="alert alert-success text-center py-3" role="alert">
                        <h4 class="alert-heading fs-5 mb-3">รหัสการเข้าใช้ห้องของคุณคือ:</h4>
                        <p class="display-6 mb-3"><strong><%= activeCode %></strong></p>
                        <p class="mt-2 small mb-0">รหัสนี้สามารถใช้ได้เพียงครั้งเดียว</p>
                    </div>
                <% } else { %>
                    <div class="alert alert-warning text-center py-3" role="alert">
                        <p class="mb-0">คุณยังไม่มีรหัสที่ใช้งานได้ กรุณาขอรหัสใหม่</p>
                    </div>
                <% } %>
                <form action="/room" method="post" class="mb-3 text-center">
                    <h4 class="mb-3 text-secondary fs-5">ขอรหัสการเข้าใช้ห้อง</h4>
                    <button type="submit" class="btn btn-primary btn-lg px-4">ยืนยัน</button>
                </form>
                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="alert alert-info text-center py-2" role="alert">
                        <small><%= message %></small>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>