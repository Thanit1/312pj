<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ข่าวสารทั้งหมด - สาขาวิศวกรรมคอมพิวเตอร์</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #f0f2f5;
        }
        .navbar {
            background-color: #3b5998 !important;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand, .nav-link {
            color: #ffffff !important;
        }
        .card {
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        .news-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 15px 15px 0 0;
        }
    </style>
</head>
<body>
    <%- include('nav', { user: user, currentPage: 'news' }) %>

    <div class="container mt-5">
        <h1 class="mb-4">ข่าวสารทั้งหมด</h1>

        <div class="row">
            <% allNews.forEach(news => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <% if (news.image_url) { %>
                            <img src="<%= news.image_url %>" alt="<%= news.title %>" class="news-image">
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title"><%= news.title %></h5>
                            <p class="card-text"><%= news.content.substring(0, 100) %>...</p>
                            <a href="/news/<%= news.id %>" class="btn btn-primary">อ่านเพิ่มเติม</a>
                        </div>
                        <div class="card-footer text-muted">
                            วันที่เผยแพร่: <%= new Date(news.created_at).toLocaleDateString('th-TH') %>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
