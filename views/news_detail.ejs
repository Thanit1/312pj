<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= newsItem.title %> - สาขาวิศวกรรมคอมพิวเตอร์</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #f0f2f5;
        }
        .navbar {
            background-color: #3b5998 !important;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand, .nav-link {
            color: #ffffff !important;
        }
        .card {
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        .news-image {
            width: 100%;
            max-height: 400px;
            object-fit: cover;
            border-radius: 15px 15px 0 0;
        }
    </style>
</head>
<body>
    <%- include('nav', { user: user, currentPage: 'news' }) %>

    <div class="container mt-5">
        <div class="card">
            <% if (newsItem.image_url) { %>
                <img src="<%= newsItem.image_url %>" alt="<%= newsItem.title %>" class="news-image">
            <% } %>
            <div class="card-body">
                <h1 class="card-title"><%= newsItem.title %></h1>
                <p class="text-muted">วันที่เผยแพร่: <%= new Date(newsItem.created_at).toLocaleDateString('th-TH') %></p>
                <div class="card-text">
                    <%= newsItem.content %>
                </div>
                <% if (newsItem.reference) { %>
                    <a href="<%= newsItem.reference %>" target="_blank" class="btn btn-outline-primary mt-3">ดูลิงค์อ้างอิง</a>
                <% } %>
            </div>
        </div>
        <a href="/computer_engineering" class="btn btn-primary mt-3">กลับไปหน้าข่าวทั้งหมด</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>